// TODO: should use chained promises to pass event blocks to clients. With each newly discovered event block, a new promise should be appended in real time

module.exports = function(startEventId, direction, cb){

		var currentEventId;

		this.start = function(){

		};

		this.stop = function(){

		};

		this.continue = function(){

		};


};